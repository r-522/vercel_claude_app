# 04_単体テスト仕様書

## 1. 単体テスト仕様書の概要

**作成日**: 2026-06-18  
**対象版**: 1.0  
**対象詳細設計書**: 03_詳細設計書.md  

本ドキュメントは、各モジュール・関数・コンポーネントの単体テストケースを定義します。

---

## 2. 前提条件・注意事項

> **重要**: 現在の `package.json` には `test` スクリプトが定義されていません。  
> 以下のテストケースを実行するには、別途テスト環境の構築が必要です。

### 2.1 テストフレームワーク（導入手順）

```bash
npm install --save-dev jest @types/jest ts-jest
npm install --save-dev @testing-library/react @testing-library/user-event @testing-library/jest-dom
npm install --save-dev jest-environment-jsdom
```

`package.json` に以下のスクリプトを追加：

```json
{
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch"
  }
}
```

### 2.2 カバレッジ目標

| 対象 | 目標 |
|------|------|
| `lib/auth/cookies.ts` | 100% |
| `lib/auth/rate-limiter.ts` | 100% |
| `lib/github/token.ts`（暗号化・復号化） | 100% |
| `lib/github/client.ts`（API クライアント） | 85%+ |
| `app/api/auth/verify/route.ts` | 90%+ |
| `app/api/chat/route.ts` | 85%+ |
| `app/api/code/route.ts`（Code モード） | 85%+ |
| `app/api/github/\*/route.ts`（GitHub routes） | 80%+ |
| コンポーネント全体 | 80%+ |
| Code 関連コンポーネント（CodeInterface, FileExplorer 等） | 75%+ |

---

## 3. Jest 設定

### 3.1 jest.config.js

```javascript
/** @type {import('jest').Config} */
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'jsdom',
  setupFilesAfterFramework: ['<rootDir>/jest.setup.ts'],
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1'
  },
  collectCoverageFrom: [
    'src/**/*.{ts,tsx}',
    '!src/**/*.d.ts',
  ],
  coverageThreshold: {
    global: { branches: 80, functions: 80, lines: 80, statements: 80 }
  }
}
```

### 3.2 jest.setup.ts

```typescript
import '@testing-library/jest-dom'

// Next.js Router のモック
jest.mock('next/navigation', () => ({
  useRouter: () => ({ replace: jest.fn(), refresh: jest.fn() }),
}))

// @ai-sdk/react useChat のモック
jest.mock('@ai-sdk/react', () => ({
  useChat: jest.fn(() => ({
    messages: [],
    sendMessage: jest.fn(),
    status: 'idle',
    error: undefined,
    regenerate: jest.fn(),
  })),
}))

// next/headers cookies のモック
jest.mock('next/headers', () => ({
  cookies: jest.fn(() => ({
    get: jest.fn(),
  })),
}))
```

---

## 4. API ルートのテストケース

### 4.1 POST /api/auth/verify

#### TC-AUTH-001: 正常系 - 正しいアクセスコード

```typescript
describe('POST /api/auth/verify', () => {
  beforeEach(() => {
    process.env.ACCESS_CODE = '1234'
    process.env.COOKIE_SECRET = 'test-secret-that-is-at-least-32chars!!'
    // rate-limiter の store をリセット（モックまたは間接呼び出し）
  })

  test('正しいコードで 200 + Set-Cookie 返却', async () => {
    const req = new Request('http://localhost/api/auth/verify', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'x-forwarded-for': '1.2.3.4' },
      body: JSON.stringify({ code: '1234' })
    })
    const res = await POST(req as NextRequest)

    expect(res.status).toBe(200)
    const body = await res.json()
    expect(body.success).toBe(true)
    expect(res.headers.get('Set-Cookie')).toContain('auth_session=')
    expect(res.headers.get('Set-Cookie')).toContain('HttpOnly')
  })
})
```

**入力**: `{ code: '1234' }`（`ACCESS_CODE=1234`）  
**期待結果**: 200、`Set-Cookie: auth_session=<JWT>...`、`{ success: true }`

---

#### TC-AUTH-002: 異常系 - 誤ったコード

```typescript
test('誤ったコードで 401 + GENERIC_ERROR 返却', async () => {
  const req = new Request('http://localhost/api/auth/verify', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'x-forwarded-for': '1.2.3.5' },
    body: JSON.stringify({ code: '9999' })
  })
  const res = await POST(req as NextRequest)

  expect(res.status).toBe(401)
  const body = await res.json()
  expect(body).toBe('コードが正しくありません')
})
```

**入力**: `{ code: '9999' }`（`ACCESS_CODE=1234`）  
**期待結果**: 401、`"コードが正しくありません"`

---

#### TC-AUTH-003: 異常系 - 無効な JSON

```typescript
test('無効な JSON で 400 返却', async () => {
  const req = new Request('http://localhost/api/auth/verify', {
    method: 'POST',
    body: 'invalid-json'
  })
  const res = await POST(req as NextRequest)
  expect(res.status).toBe(400)
})
```

---

#### TC-AUTH-004: 異常系 - ACCESS_CODE 未設定

```typescript
test('ACCESS_CODE 未設定で 500 返却', async () => {
  delete process.env.ACCESS_CODE
  const req = new Request('http://localhost/api/auth/verify', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'x-forwarded-for': '1.2.3.6' },
    body: JSON.stringify({ code: '1234' })
  })
  const res = await POST(req as NextRequest)
  expect(res.status).toBe(500)
  const body = await res.json()
  expect(body).toBe('コードが正しくありません')  // GENERIC_ERROR
})
```

---

#### TC-AUTH-005: 異常系 - レート制限超過

```typescript
test('11回目のリクエストで 429 返却', async () => {
  // 10回分のリクエストで制限まで使い切る
  for (let i = 0; i < 10; i++) {
    await POST(createVerifyRequest('9999', '10.0.0.1'))
  }
  // 11回目
  const res = await POST(createVerifyRequest('1234', '10.0.0.1'))
  expect(res.status).toBe(429)
  const body = await res.json()
  expect(body).toBe('コードが正しくありません')
})
```

**入力**: 同一 IP から11回目のリクエスト  
**期待結果**: 429、`"コードが正しくありません"`

---

### 4.2 POST /api/auth/logout

#### TC-LOGOUT-001: 正常系

```typescript
describe('POST /api/auth/logout', () => {
  test('クッキーを Max-Age=0 でクリアして 200 返却', async () => {
    const res = await POST()  // 引数なし

    expect(res.status).toBe(200)
    const body = await res.json()
    expect(body.success).toBe(true)
    expect(res.headers.get('Set-Cookie')).toContain('Max-Age=0')
    expect(res.headers.get('Set-Cookie')).toContain('auth_session=;')
  })
})
```

---

### 4.3 POST /api/chat

#### TC-CHAT-001: 正常系 - 有効なリクエスト

```typescript
describe('POST /api/chat', () => {
  test('認証済みで有効なメッセージを送信すると 200 + stream 返却', async () => {
    // verifyAuthCookie をモックして true を返す
    jest.spyOn(cookiesModule, 'verifyAuthCookie').mockResolvedValue(true)
    // streamText をモック
    const mockStream = jest.fn().mockReturnValue({ toUIMessageStreamResponse: () => new Response('ok') })
    jest.mock('ai', () => ({ streamText: mockStream }))

    const req = createChatRequest({
      messages: [{ id: '1', role: 'user', content: 'Hello' }],
      modelId: 'claude-opus-4-6',
      effort: 'high',
      thinking: true
    })
    const res = await POST(req)
    expect(res.status).toBe(200)
  })
})
```

---

#### TC-CHAT-002: 異常系 - 認証なし

```typescript
test('クッキーなしで 401 返却', async () => {
  jest.spyOn(cookiesModule, 'verifyAuthCookie').mockResolvedValue(false)
  const res = await POST(createChatRequest({ messages: [{ id: '1', role: 'user', content: 'Hi' }] }))
  expect(res.status).toBe(401)
  expect(await res.text()).toBe('Unauthorized')
})
```

---

#### TC-CHAT-003: 異常系 - 無効な modelId のフォールバック

```typescript
test('無効な modelId はデフォルト（Haiku）にフォールバック', async () => {
  jest.spyOn(cookiesModule, 'verifyAuthCookie').mockResolvedValue(true)
  let capturedModel = ''
  jest.mock('ai', () => ({
    streamText: jest.fn(({ model }) => { capturedModel = model; return { toUIMessageStreamResponse: () => new Response('') } })
  }))

  await POST(createChatRequest({
    messages: [{ id: '1', role: 'user', content: 'Hi' }],
    modelId: 'invalid-model-xyz'
  }))

  expect(capturedModel).toBe(DEFAULT_MODEL_ID)  // Haiku
})
```

---

#### TC-CHAT-004: 異常系 - messages 空配列

```typescript
test('messages 空配列で 400 返却', async () => {
  jest.spyOn(cookiesModule, 'verifyAuthCookie').mockResolvedValue(true)
  const res = await POST(createChatRequest({ messages: [] }))
  expect(res.status).toBe(400)
})
```

---

#### TC-CHAT-005: thinking / temperature 排他の確認

```typescript
test('Opus で thinking=true のとき providerOptions が設定され temperature が未設定', async () => {
  jest.spyOn(cookiesModule, 'verifyAuthCookie').mockResolvedValue(true)
  let capturedOptions: any = null
  jest.mock('ai', () => ({
    streamText: jest.fn((opts) => { capturedOptions = opts; return { toUIMessageStreamResponse: () => new Response('') } })
  }))

  await POST(createChatRequest({
    messages: [{ id: '1', role: 'user', content: 'Think' }],
    modelId: 'claude-opus-4-6',
    effort: 'high',
    thinking: true
  }))

  expect(capturedOptions.providerOptions?.anthropic?.thinking?.type).toBe('enabled')
  expect(capturedOptions.temperature).toBeUndefined()
})

test('Haiku で thinking=true でも temperature が設定される（thinking 非対応）', async () => {
  jest.spyOn(cookiesModule, 'verifyAuthCookie').mockResolvedValue(true)
  let capturedOptions: any = null
  jest.mock('ai', () => ({
    streamText: jest.fn((opts) => { capturedOptions = opts; return { toUIMessageStreamResponse: () => new Response('') } })
  }))

  await POST(createChatRequest({
    messages: [{ id: '1', role: 'user', content: 'Test' }],
    modelId: 'claude-haiku-4-5-20251001',
    effort: 'high',
    thinking: true  // ← Haiku は supportsThinking=false なので無効
  }))

  expect(capturedOptions.providerOptions).toBeUndefined()
  expect(typeof capturedOptions.temperature).toBe('number')
})
```

#### TC-CHAT-006: Web 検索ツールの付与判定

```typescript
test('webSearch=true のとき web_search ツールが付与される', async () => {
  jest.spyOn(cookiesModule, 'verifyAuthCookie').mockResolvedValue(true)
  let capturedOptions: any = null
  jest.mock('ai', () => ({
    streamText: jest.fn((opts) => { capturedOptions = opts; return { toUIMessageStreamResponse: () => new Response('') } })
  }))

  await POST(createChatRequest({
    messages: [{ id: '1', role: 'user', content: '最新ニュースは？' }],
    modelId: 'claude-haiku-4-5-20251001',
    effort: 'high',
    webSearch: true
  }))

  expect(capturedOptions.tools?.web_search).toBeDefined()
})

test('webSearch 省略時は tools が未設定', async () => {
  jest.spyOn(cookiesModule, 'verifyAuthCookie').mockResolvedValue(true)
  let capturedOptions: any = null
  jest.mock('ai', () => ({
    streamText: jest.fn((opts) => { capturedOptions = opts; return { toUIMessageStreamResponse: () => new Response('') } })
  }))

  await POST(createChatRequest({
    messages: [{ id: '1', role: 'user', content: 'Test' }],
    modelId: 'claude-haiku-4-5-20251001',
    effort: 'high'
    // webSearch 未指定 → applyWebSearch=false
  }))

  expect(capturedOptions.tools).toBeUndefined()
})

test('webSearch が boolean 以外（"true" 文字列）の場合は無効（厳密 === true 判定）', async () => {
  jest.spyOn(cookiesModule, 'verifyAuthCookie').mockResolvedValue(true)
  let capturedOptions: any = null
  jest.mock('ai', () => ({
    streamText: jest.fn((opts) => { capturedOptions = opts; return { toUIMessageStreamResponse: () => new Response('') } })
  }))

  await POST(createChatRequest({
    messages: [{ id: '1', role: 'user', content: 'Test' }],
    modelId: 'claude-haiku-4-5-20251001',
    effort: 'high',
    webSearch: 'true' as any  // ← 文字列はサーバーで無効化される
  }))

  expect(capturedOptions.tools).toBeUndefined()
})
```

---

## 5. ユーティリティ関数のテストケース

### 5.1 lib/auth/cookies.ts

#### TC-JWT-001: JWT 生成と検証

```typescript
describe('cookies.ts', () => {
  beforeEach(() => {
    process.env.COOKIE_SECRET = 'this-is-a-32-char-minimum-secret!!'
  })

  test('signAuthCookie → verifyAuthCookie で true', async () => {
    const token = await signAuthCookie()
    expect(typeof token).toBe('string')
    const valid = await verifyAuthCookie(token)
    expect(valid).toBe(true)
  })
})
```

---

#### TC-JWT-002: 無効なトークン

```typescript
test('改ざんされたトークンで false', async () => {
  const valid = await verifyAuthCookie('invalid.token.here')
  expect(valid).toBe(false)
})
```

---

#### TC-JWT-003: COOKIE_SECRET 未設定

```typescript
test('COOKIE_SECRET 未設定で getSecret() が例外', () => {
  delete process.env.COOKIE_SECRET
  expect(() => getSecret()).toThrow()
})
```

---

#### TC-JWT-004: COOKIE_SECRET 32文字未満

```typescript
test('COOKIE_SECRET が 32文字未満で getSecret() が例外', () => {
  process.env.COOKIE_SECRET = 'short'
  expect(() => getSecret()).toThrow()
})
```

---

#### TC-COOKIE-005: buildCookieHeader の属性確認

```typescript
test('buildCookieHeader は HttpOnly / SameSite=Lax を含む', async () => {
  const token = await signAuthCookie()
  const header = buildCookieHeader(token)
  expect(header).toContain('HttpOnly')
  expect(header).toContain('SameSite=Lax')
  expect(header).toContain('Max-Age=2592000')
  expect(header).toContain('Path=/')
})

test('buildCookieHeader は非本番環境では Secure を含まない', async () => {
  process.env.NODE_ENV = 'test'
  const token = await signAuthCookie()
  const header = buildCookieHeader(token)
  expect(header).not.toContain('Secure')
})
```

---

#### TC-COOKIE-006: buildClearCookieHeader

```typescript
test('buildClearCookieHeader は Max-Age=0 を含む', () => {
  const header = buildClearCookieHeader()
  expect(header).toContain('Max-Age=0')
  expect(header).toContain('auth_session=;')
})
```

---

### 5.2 lib/auth/rate-limiter.ts

#### TC-RATE-001: 10回以内は許可

```typescript
describe('rate-limiter', () => {
  beforeEach(() => {
    // store をリセット（モジュールを再 require するか、store をクリアする関数を公開）
    jest.resetModules()
  })

  test('10回のリクエストはすべて allowed=true', () => {
    const ip = '192.168.0.1'
    for (let i = 1; i <= 10; i++) {
      const result = checkRateLimit(ip)
      expect(result.allowed).toBe(true)
      expect(result.remaining).toBe(10 - i)
    }
  })
})
```

---

#### TC-RATE-002: 11回目は拒否

```typescript
test('11回目は allowed=false, remaining=0', () => {
  const ip = '192.168.0.2'
  for (let i = 0; i < 10; i++) checkRateLimit(ip)
  const result = checkRateLimit(ip)
  expect(result.allowed).toBe(false)
  expect(result.remaining).toBe(0)
})
```

---

#### TC-RATE-003: 15分後にリセット

```typescript
test('15分後は制限がリセットされる', () => {
  jest.useFakeTimers()
  const ip = '192.168.0.3'
  for (let i = 0; i < 10; i++) checkRateLimit(ip)
  // 15分+1ms 進める
  jest.advanceTimersByTime(RATE_LIMIT_WINDOW_MS + 1)
  const result = checkRateLimit(ip)
  expect(result.allowed).toBe(true)
  expect(result.remaining).toBe(9)
  jest.useRealTimers()
})
```

---

#### TC-RATE-004: IP 別に独立してカウント

```typescript
test('異なる IP は独立してカウントされる', () => {
  const ip1 = '10.0.0.1'
  const ip2 = '10.0.0.2'
  for (let i = 0; i < 10; i++) checkRateLimit(ip1)
  const result = checkRateLimit(ip2)
  expect(result.allowed).toBe(true)
})
```

---

## 6. React コンポーネントのテストケース

### 6.1 AuthForm.tsx

#### TC-COMP-AUTH-001: 初期表示

```typescript
describe('AuthForm', () => {
  test('4つの数字入力フィールドが表示される', () => {
    const { getAllByRole } = render(<AuthForm />)
    const inputs = getAllByRole('textbox')
    expect(inputs).toHaveLength(4)
  })

  test('最初の入力にフォーカスが当たる', () => {
    const { getAllByRole } = render(<AuthForm />)
    expect(getAllByRole('textbox')[0]).toHaveFocus()
  })
})
```

---

#### TC-COMP-AUTH-002: 桁入力とフォーカス移動

```typescript
test('1桁入力で次のフィールドにフォーカスが移動する', async () => {
  const { getAllByRole } = render(<AuthForm />)
  const inputs = getAllByRole('textbox')
  await userEvent.type(inputs[0], '1')
  expect(inputs[1]).toHaveFocus()
})
```

---

#### TC-COMP-AUTH-003: 4桁入力で自動送信

```typescript
test('4桁入力完了で POST /api/auth/verify が呼ばれる', async () => {
  const mockFetch = jest.fn().mockResolvedValue({
    ok: false, json: async () => 'コードが正しくありません'
  })
  global.fetch = mockFetch

  const { getAllByRole } = render(<AuthForm />)
  const inputs = getAllByRole('textbox')
  await userEvent.type(inputs[0], '1')
  await userEvent.type(inputs[1], '2')
  await userEvent.type(inputs[2], '3')
  await userEvent.type(inputs[3], '4')

  expect(mockFetch).toHaveBeenCalledWith(
    '/api/auth/verify',
    expect.objectContaining({
      method: 'POST',
      body: JSON.stringify({ code: '1234' })
    })
  )
})
```

---

#### TC-COMP-AUTH-004: ペースト対応

```typescript
test('ペーストで4桁を一括入力できる', async () => {
  const mockFetch = jest.fn().mockResolvedValue({
    ok: false, json: async () => 'コードが正しくありません'
  })
  global.fetch = mockFetch

  const { getAllByRole } = render(<AuthForm />)
  const inputs = getAllByRole('textbox')
  fireEvent.paste(inputs[0], {
    clipboardData: { getData: () => '5678' }
  })
  expect(mockFetch).toHaveBeenCalledWith(
    '/api/auth/verify',
    expect.objectContaining({ body: JSON.stringify({ code: '5678' }) })
  )
})
```

---

### 6.2 ChatInterface.tsx

#### TC-COMP-CHAT-001: 初期レンダリング

```typescript
describe('ChatInterface', () => {
  test('ヘッダ・MessageList・InputArea が表示される', () => {
    const { getByText } = render(<ChatInterface />)
    expect(getByText(/claude-.*-app/)).toBeInTheDocument()
    expect(getByText('Sign out')).toBeInTheDocument()
  })
})
```

---

#### TC-COMP-CHAT-002: Haiku 選択時のギアボタンで ModelSettings 表示

```typescript
test('ギアボタンで ModelSettings パネルが表示される', async () => {
  const { getByLabelText, getByRole } = render(<ChatInterface />)
  await userEvent.click(getByLabelText('Settings'))
  expect(getByRole('radiogroup')).toBeInTheDocument()
})
```

---

### 6.3 ModelSettings.tsx

#### TC-COMP-SETTINGS-001: Haiku 時のゲーティング

```typescript
describe('ModelSettings', () => {
  test('supportsThinking=false で low 以外の Effort が disabled', () => {
    const { getAllByRole } = render(
      <ModelSettings
        effort="high"
        thinking={false}
        supportsThinking={false}
        webSearch={false}
        onEffortChange={jest.fn()}
        onThinkingChange={jest.fn()}
        onWebSearchChange={jest.fn()}
        onClose={jest.fn()}
      />
    )
    const radios = getAllByRole('radio')
    // low は有効、他は disabled
    expect(radios[0]).not.toBeDisabled()  // low
    expect(radios[1]).toBeDisabled()       // medium
    expect(radios[2]).toBeDisabled()       // high
    expect(radios[3]).toBeDisabled()       // xhigh
    expect(radios[4]).toBeDisabled()       // max
  })

  test('supportsThinking=true で全 Effort が有効', () => {
    const { getAllByRole } = render(
      <ModelSettings
        effort="high"
        thinking={false}
        supportsThinking={true}
        webSearch={false}
        onEffortChange={jest.fn()}
        onThinkingChange={jest.fn()}
        onWebSearchChange={jest.fn()}
        onClose={jest.fn()}
      />
    )
    const radios = getAllByRole('radio')
    radios.forEach(r => expect(r).not.toBeDisabled())
  })
})
```

#### TC-COMP-SETTINGS-002: Web 検索トグル

```typescript
describe('ModelSettings - Web検索トグル', () => {
  const baseProps = {
    effort: 'high' as const,
    thinking: false,
    supportsThinking: false,  // Haiku でも
    webSearch: false,
    onEffortChange: jest.fn(),
    onThinkingChange: jest.fn(),
    onWebSearchChange: jest.fn(),
    onClose: jest.fn(),
  }

  test('Web検索トグルは全モデルで有効（disabled でない）', () => {
    const { getByLabelText } = render(<ModelSettings {...baseProps} />)
    const toggle = getByLabelText('Web検索を有効にする')
    expect(toggle).not.toBeDisabled()
    expect(toggle).toHaveAttribute('aria-checked', 'false')
  })

  test('クリックで onWebSearchChange(true) が呼ばれる', () => {
    const onWebSearchChange = jest.fn()
    const { getByLabelText } = render(
      <ModelSettings {...baseProps} onWebSearchChange={onWebSearchChange} />
    )
    fireEvent.click(getByLabelText('Web検索を有効にする'))
    expect(onWebSearchChange).toHaveBeenCalledWith(true)
  })

  test('webSearch=true のとき aria-checked=true', () => {
    const { getByLabelText } = render(
      <ModelSettings {...baseProps} webSearch={true} />
    )
    expect(getByLabelText('Web検索を有効にする')).toHaveAttribute('aria-checked', 'true')
  })
})
```

---

### 6.4 MessageList.tsx

#### TC-COMP-LIST-001: メッセージ表示

```typescript
describe('MessageList', () => {
  test('messages が表示される', () => {
    const messages = [
      { id: '1', role: 'user', content: 'Hello', parts: [{ type: 'text', text: 'Hello' }] },
      { id: '2', role: 'assistant', content: 'Hi', parts: [{ type: 'text', text: 'Hi' }] },
    ] as UIMessage[]

    const { getByText } = render(<MessageList messages={messages} isStreaming={false} />)
    expect(getByText('Hello')).toBeInTheDocument()
    expect(getByText('Hi')).toBeInTheDocument()
  })

  test('messages が空のとき "Enter a query to begin" が表示される', () => {
    const { getByText } = render(<MessageList messages={[]} isStreaming={false} />)
    expect(getByText('Enter a query to begin')).toBeInTheDocument()
  })
})
```

---

### 6.5 MessageItem.tsx

#### TC-COMP-ITEM-001: ユーザーメッセージ表示

```typescript
describe('MessageItem', () => {
  test('ユーザーメッセージに "Query" ラベルが表示される', () => {
    const message = {
      id: '1', role: 'user',
      parts: [{ type: 'text', text: 'Hello' }]
    } as UIMessage

    const { getByText } = render(<MessageItem message={message} isStreaming={false} />)
    expect(getByText('Query')).toBeInTheDocument()
    expect(getByText('Hello')).toBeInTheDocument()
  })

  test('アシスタントメッセージに "Result" ラベルが表示される', () => {
    const message = {
      id: '2', role: 'assistant',
      parts: [{ type: 'text', text: 'Response' }]
    } as UIMessage

    const { getByText } = render(<MessageItem message={message} isStreaming={false} />)
    expect(getByText('Result')).toBeInTheDocument()
  })

  test('isStreaming かつ content なし → LoadingDots 表示', () => {
    const message = {
      id: '3', role: 'assistant',
      parts: []
    } as UIMessage

    const { container } = render(<MessageItem message={message} isStreaming={true} />)
    // LoadingDots の存在確認（クラス名やテスト ID で）
    expect(container.querySelector('.loading-dots, [data-testid="loading-dots"]')).toBeTruthy()
  })
})
```

---

### 6.6 InputArea.tsx

#### TC-COMP-INPUT-001: Enter で送信

```typescript
describe('InputArea', () => {
  test('Enter で onSubmit が呼ばれる', async () => {
    const onSubmit = jest.fn()
    const { getByRole } = render(
      <InputArea value="Hello" disabled={false} onChange={jest.fn()} onSubmit={onSubmit} />
    )
    const textarea = getByRole('textbox')
    await userEvent.type(textarea, '{Enter}')
    expect(onSubmit).toHaveBeenCalled()
  })

  test('Shift+Enter では onSubmit が呼ばれない（改行）', async () => {
    const onSubmit = jest.fn()
    const { getByRole } = render(
      <InputArea value="Hello" disabled={false} onChange={jest.fn()} onSubmit={onSubmit} />
    )
    const textarea = getByRole('textbox')
    await userEvent.type(textarea, '{Shift>}{Enter}{/Shift}')
    expect(onSubmit).not.toHaveBeenCalled()
  })
})
```

---

### 6.7 CodeBlock.tsx

#### TC-COMP-CODE-001: コピーボタン

```typescript
describe('CodeBlock', () => {
  test('コピーボタンクリックでクリップボードに書き込まれる', async () => {
    const mockWrite = jest.fn()
    Object.assign(navigator, { clipboard: { writeText: mockWrite } })

    const { getByText } = render(<CodeBlock language="js">const x = 1</CodeBlock>)
    await userEvent.click(getByText('コピー'))
    expect(mockWrite).toHaveBeenCalledWith('const x = 1')
  })

  test('6行以下では行番号が表示されない', () => {
    const code = 'line1\nline2\nline3'
    const { container } = render(<CodeBlock language="js">{code}</CodeBlock>)
    // showLineNumbers=false の場合、行番号要素がないことを確認
    expect(container.querySelector('.linenumber')).toBeNull()
  })

  test('6行超では行番号が表示される', () => {
    const code = 'l1\nl2\nl3\nl4\nl5\nl6\nl7'
    const { container } = render(<CodeBlock language="js">{code}</CodeBlock>)
    expect(container.querySelector('.linenumber')).toBeTruthy()
  })
})
```

---

### 6.8 proxy.ts（isPublicPath）

#### TC-PROXY-001: パブリックパス判定

```typescript
describe('proxy.ts isPublicPath', () => {
  test('/auth はパブリックパス', () => {
    expect(isPublicPath('/auth')).toBe(true)
  })

  test('/api/auth/verify はパブリックパス', () => {
    expect(isPublicPath('/api/auth/verify')).toBe(true)
  })

  test('/api/auth/logout はパブリックパス', () => {
    expect(isPublicPath('/api/auth/logout')).toBe(true)
  })

  test('/ は保護パス', () => {
    expect(isPublicPath('/')).toBe(false)
  })

  test('/api/chat は保護パス', () => {
    expect(isPublicPath('/api/chat')).toBe(false)
  })
})
```

---

### 6.9 useDarkMode フック

#### TC-HOOK-DARK-001: ダークモード切替

```typescript
describe('useDarkMode', () => {
  beforeEach(() => {
    document.documentElement.classList.remove('dark')
    localStorage.clear()
  })

  test('toggle() でダークモードになる', () => {
    const { result } = renderHook(() => useDarkMode())
    act(() => { result.current.toggle() })
    expect(document.documentElement.classList.contains('dark')).toBe(true)
    expect(localStorage.getItem('theme')).toBe('dark')
  })

  test('ダークモードから toggle() でライトモードになる', () => {
    document.documentElement.classList.add('dark')
    const { result } = renderHook(() => useDarkMode())
    act(() => { result.current.toggle() })
    expect(document.documentElement.classList.contains('dark')).toBe(false)
    expect(localStorage.getItem('theme')).toBe('light')
  })
})
```

---

### 6.10 useImageAttachments フック

#### TC-HOOK-IMG-001: 画像添付管理

```typescript
describe('useImageAttachments', () => {
  test('add で画像が追加される', () => {
    const file = new File(['dummy'], 'test.png', { type: 'image/png' })
    const { result } = renderHook(() => useImageAttachments())
    act(() => { result.current.add([file]) })
    expect(result.current.attached).toHaveLength(1)
    expect(result.current.attached[0].file).toBe(file)
  })

  test('image/* 以外のファイルは無視される', () => {
    const file = new File(['dummy'], 'test.pdf', { type: 'application/pdf' })
    const { result } = renderHook(() => useImageAttachments())
    act(() => { result.current.add([file]) })
    expect(result.current.attached).toHaveLength(0)
  })

  test('remove で特定の画像が削除される', () => {
    const file1 = new File(['a'], 'a.png', { type: 'image/png' })
    const file2 = new File(['b'], 'b.png', { type: 'image/png' })
    const { result } = renderHook(() => useImageAttachments())
    act(() => { result.current.add([file1, file2]) })
    act(() => { result.current.remove(0) })
    expect(result.current.attached).toHaveLength(1)
    expect(result.current.attached[0].file).toBe(file2)
  })

  test('clear で全て削除される', () => {
    const file = new File(['a'], 'a.png', { type: 'image/png' })
    const { result } = renderHook(() => useImageAttachments())
    act(() => { result.current.add([file]) })
    act(() => { result.current.clear() })
    expect(result.current.attached).toHaveLength(0)
  })
})
```

---

## 7. GitHub 統合機能のテストケース

### 7.1 lib/github/token.ts — トークン暗号化・復号化

#### TC-GH-TOKEN-001: トークン暗号化

```typescript
describe('encryptGitHubToken', () => {
  test('正常にトークンを暗号化する', async () => {
    const token = 'ghp_test_token_xyz'
    const encrypted = await encryptGitHubToken(token)
    expect(typeof encrypted).toBe('string')
    expect(encrypted).not.toBe(token) // 暗号化されている
  })

  test('同じトークンでも毎回異なる暗号文になる（nonce のため）', async () => {
    const token = 'ghp_test_token_xyz'
    const enc1 = await encryptGitHubToken(token)
    const enc2 = await encryptGitHubToken(token)
    expect(enc1).not.toBe(enc2) // JWE の nonce で異なる
  })
})
```

#### TC-GH-TOKEN-002: トークン復号化

```typescript
describe('decryptGitHubToken', () => {
  test('正常に暗号化トークンを復号化する', async () => {
    const original = 'ghp_test_token_xyz'
    const encrypted = await encryptGitHubToken(original)
    const decrypted = await decryptGitHubToken(encrypted)
    expect(decrypted).toBe(original)
  })

  test('不正な暗号文を復号化しようとすると null を返す', async () => {
    const result = await decryptGitHubToken('invalid_jwe_string')
    expect(result).toBeNull()
  })
})
```

### 7.2 POST /api/github/callback — OAuth コールバック・トークン交換

#### TC-GH-CALLBACK-001: 正常系 — code を token と交換

```typescript
describe('GET /api/github/callback', () => {
  beforeEach(() => {
    process.env.GITHUB_CLIENT_ID = 'test_client_id'
    process.env.GITHUB_CLIENT_SECRET = 'test_client_secret'
  })

  test('code が token と交換され、Set-Cookie で返される', async () => {
    global.fetch = jest.fn(() =>
      Promise.resolve({
        ok: true,
        json: async () => ({ access_token: 'ghp_test_token' })
      })
    ) as jest.Mock

    const req = new Request('http://localhost/api/github/callback?code=auth_code_xyz')
    const res = await GET(req)
    
    expect(res.status).toBe(302)
    expect(res.headers.get('location')).toBe('/')
    expect(res.headers.get('set-cookie')).toContain('github_token=')
  })
})
```

#### TC-GH-CALLBACK-002: 異常系 — code が無い

```typescript
test('code パラメータなしで 400 を返す', async () => {
  const req = new Request('http://localhost/api/github/callback')
  const res = await GET(req)
  expect(res.status).toBe(400)
  expect(await res.text()).toContain('No code provided')
})
```

### 7.3 GET /api/github/status — 接続状態確認

#### TC-GH-STATUS-001: 接続状態チェック

```typescript
describe('GET /api/github/status', () => {
  test('token が無い場合 connected: false を返す', async () => {
    const req = new Request('http://localhost/api/github/status')
    const res = await GET(req)
    const json = await res.json()
    expect(json.connected).toBe(false)
  })

  test('有効な token がある場合 connected: true + user info を返す', async () => {
    // github_token cookie をモック
    global.fetch = jest.fn(() =>
      Promise.resolve({
        ok: true,
        json: async () => ({ login: 'testuser', avatar_url: 'https://...' })
      })
    ) as jest.Mock

    // request に github_token cookie をセット
    const req = new Request('http://localhost/api/github/status')
    const res = await GET(req)
    const json = await res.json()
    
    expect(json.connected).toBe(true)
    expect(json.user.login).toBe('testuser')
  })
})
```

### 7.4 POST /api/code — Code モード チャット

#### TC-GH-CODE-001: コンテキスト付きチャット

```typescript
describe('POST /api/code', () => {
  beforeEach(() => {
    process.env.AUTH_COOKIE_NAME = 'auth_session'
    // GitHub token を mock cookie に設定
  })

  test('リポジトリ + ファイルコンテキストを含むメッセージが送信される', async () => {
    const req = new Request('http://localhost/api/code', {
      method: 'POST',
      body: JSON.stringify({
        messages: [{ role: 'user', content: 'このコードを修正して' }],
        repo: 'user/repo',
        ref: 'main',
        files: [{ path: 'src/app.ts' }],
        modelId: 'claude-opus-4-6',
        effort: 'high',
        thinking: false
      })
    })
    
    const res = await POST(req)
    expect(res.status).toBe(200)
    // ストリーミングレスポンスが返される
  })

  test('GitHub token が無い場合 401 を返す', async () => {
    const req = new Request('http://localhost/api/code', {
      method: 'POST',
      body: JSON.stringify({ messages: [] })
    })
    const res = await POST(req)
    expect(res.status).toBe(401)
  })
})
```

### 7.5 useGitHub フック

#### TC-GH-HOOK-001: GitHub API ラッパー

```typescript
describe('useGitHub', () => {
  test('getRepositories で リポジトリ一覧を取得する', async () => {
    global.fetch = jest.fn(() =>
      Promise.resolve({
        ok: true,
        json: async () => [
          { id: 1, name: 'repo1', full_name: 'user/repo1' },
          { id: 2, name: 'repo2', full_name: 'user/repo2' }
        ]
      })
    ) as jest.Mock

    const { result } = renderHook(() => useGitHub())
    const repos = await result.current.getRepositories('token')
    
    expect(repos).toHaveLength(2)
    expect(repos[0].full_name).toBe('user/repo1')
  })

  test('getBranches で 指定リポジトリのブランチを取得する', async () => {
    global.fetch = jest.fn(() =>
      Promise.resolve({
        ok: true,
        json: async () => [
          { name: 'main', commit: { sha: 'abc...' } },
          { name: 'develop', commit: { sha: 'def...' } }
        ]
      })
    ) as jest.Mock

    const { result } = renderHook(() => useGitHub())
    const branches = await result.current.getBranches('token', 'user/repo')
    
    expect(branches).toHaveLength(2)
    expect(branches[0].name).toBe('main')
  })
})
```

---

## 9. テスト実行スケジュール

| フェーズ | タイミング | 担当 |
|---------|----------|------|
| ユニットテスト | コミット前（手動） | 開発者 |
| ユニットテスト（CI） | PR 作成時（自動） | CI/CD |
| カバレッジレビュー | マージ前 | QA |

---

## 10. 変更履歴

| 版 | 日付 | 変更内容 |
|----|------|--------|
| 1.0 | 2026-06-18 | 初版作成 |
| 1.1 | 2026-06-20 | GitHub 統合機能のテストケース（TC-GH-\*）を追加 |
